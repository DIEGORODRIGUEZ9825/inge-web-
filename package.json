{
  "name": "autos-darm",
  "version": "1.0.0",
  "description": "Servidor y entorno de pruebas para Autos DARM con JSON Server",
  "main": "db.json",
 "scripts": {
  "start": "json-server --watch db.json --port 3002",
  "reset-db": "node scripts/reset-db.js",
  "test": "jest --verbose --runInBand"
},

  "dependencies": {
    "axios": "^1.13.0",
    "json-server": "^1.0.0-beta.3"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "supertest": "^7.1.4"
  }
}
